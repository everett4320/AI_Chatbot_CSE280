# Copy to .env and fill in when you have a Gemini key.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# Optional (local dev defaults are set in docker-compose.yml).
DATA_RETENTION_DAYS=30
ENABLE_DEV_ENDPOINTS=1
